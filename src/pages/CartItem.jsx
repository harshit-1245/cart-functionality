import React from 'react'
import { ShopContext } from '../shopContext/shopContext';

const CartItems = ({data}) => {
    const {cartItem,addItemToCart}=useContext(ShopContext);
  return (
    <div>
      
    </div>
  )
}

export default CartItems
